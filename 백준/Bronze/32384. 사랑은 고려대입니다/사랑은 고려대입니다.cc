#include <stdio.h>

void sol(int n) {
    for (int i = 0; i < n; i++) {
        printf("LoveisKoreaUniversity ");
    }
    printf("\n");
}

int main() {
    int n;
    scanf("%d", &n);
    sol(n);
    return 0;
}
