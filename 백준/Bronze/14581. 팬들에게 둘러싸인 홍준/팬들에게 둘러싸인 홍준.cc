#include <stdio.h>

int main() {
    char id[100];
    scanf("%s", id);

    printf(":fan::fan::fan:\n");
    printf(":fan::%s::fan:\n", id);
    printf(":fan::fan::fan:\n");

    return 0;
}